#include "main.h"
/**
 * _abs - computes the absolute value of an int
 * @n: the int in question
 * Return: the absolut value of the int
 */

int _abs(int n)
{
	if (n >= o)
		return (n);
	else
		return (-n);
}
